program notenberechnung
global
	const pkt:ratio;
	const max:ratio;

	fun test(copy const min:ratio) returns a:ratio
	do
		debugout min;
		a := min + 1
	endfun
do
	pkt := 37;
	max := 51;

	//debugout calculate(pkt, max)
	//call test(pkt)
	debugout test(pkt)
endprogram
