program SimpleFunc
global
  const y:ratio;
  //const y:int;
  //const b:bool;
  //const i:int;
  //proc incrZ (m:bool)
  //local
  //  var k:ratio
  //do
  //  skip
    //x := (k + 1) + i + j
  //endproc;

  //fun getZ () returns zx:ratio
//  local
//    var g:bool
  //do
//    skip //zx := z
  //endfun
//;
  proc procZ (i:int)
  local
    var x:ratio
  do
    skip
  endproc

  //;proc next()
  //do
//    skip
//  endproc
do
  // debugin z;
  // debugout denum z;
  // debugout not b;
  // debugout -i;
  // debugout +i;
  // debugout (z > i) = b;
  // debugout (z > i) = z;
  // debugout getZ();
  // call getZ()
  debugin y;
  debugout y
endprogram
