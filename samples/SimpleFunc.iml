program SimpleFunc
global
  const z:ratio;
  const b:bool;
  const i:int;
  proc incrZ (m:bool)
  local
    var k:ratio
  do
    skip
    //x := (k + 1) + i + j
  endproc
do
  // debugin z;
  // debugout denum z;
  // debugout not b;
  // debugout -i;
  // debugout +i;
  // debugout (z > i) = b;
  debugout (z > i) = z
endprogram
