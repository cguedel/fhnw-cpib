program SimpleFunc
global
  const z:ratio;
  const y:int
  //const b:bool;
  //const i:int;
  //proc incrZ (m:bool)
  //local
  //  var k:ratio
  //do
  //  skip
    //x := (k + 1) + i + j
  //endproc;

  //fun getZ () returns zx:ratio
  //do
  //  zx := z
  //endfun;

  //proc procZ ()
  //do
  //  skip
  //endproc
do
  // debugin z;
  // debugout denum z;
  // debugout not b;
  // debugout -i;
  // debugout +i;
  // debugout (z > i) = b;
  // debugout (z > i) = z;
  // debugout getZ();
  // call getZ()
  skip
endprogram
